version: 1.0
runtime: python311

build:
  # Apenas uma string. Nada de lista.
  command: "echo 'Build step - nothing to do'"

run:
  runtime-version: 3.11

  # Aqui deve ser lista → OK
  pre-run:
    - pip3 install --no-cache-dir -r requirements.txt

  command: "python3 app.py"

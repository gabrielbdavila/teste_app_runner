version: 1.0
runtime: python311

run:
  runtime-version: 3.11

  # Instala as dependências DENTRO da imagem de runtime (correto!)
  pre-run:
    - pip3 install --no-cache-dir -r requirements.txt

  # Comando para iniciar seu aplicativo Dash
  command: python3 app.py

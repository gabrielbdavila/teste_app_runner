runtime: python311

build:
  commands:
    # Nenhum passo de build necessário por enquanto
    - echo "Build step - nothing to do"

run:
  runtime-version: 3.11

  # Aqui é onde instalamos as dependências DENTRO da imagem de runtime
  pre-run:
    - pip3 install --no-cache-dir -r requirements.txt

  # Comando que inicia sua aplicação
  command: python3 app.py
